services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - local.env
  db:
    image: postgres:15
    env_file:
      - local.env
    ports:
      - "5432:5432"